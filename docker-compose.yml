version: '3'

services:
  api:
  #Looks for a dockerfile and starts the dockerfile
    build: .
    #HOST:CONTAINER, 8000 (my laptop,e.g)  →  8000 (inside Docker container)
    ports:
      - "8000:8000"
    environment:
      - DATABASE_HOSTNAME=postgres
      - DATABASE_PORT=5432
      - DATABASE_PASSWORD=Password123
      - DATABASE_NAME=Fastapi
      - DATABASE_USERNAME=postgres
      - SECRET_KEY=your_secret
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=300
    depends_on:
      - postgres

  postgres:
  #Doesn't need dockerfile, it is from the sky, like dockerhub, it is already made, just needs to be downloaded
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: Password123
      POSTGRES_DB: Fastapi
    ports:
      - "5432:5432"